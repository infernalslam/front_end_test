// This getter is a function which just returns the count
// With ES6 you can also write it as:
export const getState = state => state.count

// export function getCount (state) {
//   return state.count
// }
